'use strict';
const Node =  require('../SLLNode.js');
const LinkedList = require('../LinkedList.js');

// Require our linked list implementation

describe('LinkedList', () => {
  it('works', () => {
    expect(true).toBeTruthy();
  });
});

describe('LinkedList', () => {
  it('should append a value to the end of the list', () => {

    // let nodeX = new Node(1);
    // nodeX.next = new Node(2);
    // let linkedList = new LinkedList(nodeX);
    //
    //
    console.log("SIZE",linkedList.iterate());
    expect(0).toBeTruthy();
  });

  // it('should return the head of the list', () => {
  //   const linkedList = new LinkedList();
  //   linkedList.append(1);
  //   linkedList.append(2);
  //
  //   expect(linkedList.head.value).toBe(1);
  // });

  // ... other tests
});